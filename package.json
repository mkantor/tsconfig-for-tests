{
  "name": "",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc --build tsconfig.app.json",
    "clean": "rm -rf dist* *.tsbuildinfo",
    "start": "npm run build && node ./dist/main.js",
    "test": "tsc --project tsconfig.app.json --outDir dist-test --declarationDir dist && tsc --build tsconfig.test.json && node ./dist-test/main.test.js"
  },
  "devDependencies": {
    "@types/node": "^20.6.3",
    "typescript": "^5.6.2"
  }
}
